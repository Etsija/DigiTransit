query StopsByRadius($lat: Float!, $lon: Float!, $radius: Int!) {
    stopsByRadius(lat: $lat, lon: $lon, radius: $radius) {
        edges {
            node {
                stop {
                    id
                    gtfsId
                    name
                    code
                    zoneId
                    patterns {
                        id
                        name
                    }
                    vehicleType
                }
                distance
            }
        }
    }
}